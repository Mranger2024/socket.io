{
  "install": "pnpm install",
  "start": "pnpm run build && pnpm start",
  "watch": {
    "install": {
      "include": [
        "^package\\.json$"
      ]
    },
    "restart": {
      "include": [
        "^dist/"
      ]
    },
    "throttle": 1000
  }
}
